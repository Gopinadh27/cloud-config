aws:
  secretsmanager:
    host: ${AWS_SECRETS_MANAGER_HOST}
    accesskey: ${AWS_SECRETS_MANAGER_ACCESS_KEY}
    secretkey: ${AWS_SECRETS_MANAGER_SCRET_KEY}
    region: ${AWS_SECRETS_MANAGER_REGION}

  profile:
    msg: 'Activated local profile'
